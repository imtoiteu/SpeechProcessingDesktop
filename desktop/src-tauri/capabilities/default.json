{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the STTLive launcher window (core APIs only; no plugins).",
  "windows": ["main"],
  "permissions": ["core:default"]
}
