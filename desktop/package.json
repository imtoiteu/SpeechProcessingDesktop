{
  "name": "sttlive-desktop",
  "version": "0.1.0",
  "private": true,
  "description": "Native desktop launcher/wrapper for the STTLive STT + TTS servers (Tauri v2).",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "icon": "tauri icon ./app-icon.png"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
